# Phase B — Local prototypes

This folder contains:
- notebooks/cv_toy.ipynb
- notebooks/qubit_toy.ipynb
- src/cv_phase.py
- src/trotter_chain.py
- tests/test_phaseB.py

## How to run (VS Code)
1. Create and activate a Python 3.10+ venv; `pip install numpy matplotlib pytest`.
2. Open the notebooks and run all cells.
3. Run tests: `pytest -q` inside the sec5 parent.
All code is NumPy-only (no SciPy required).
